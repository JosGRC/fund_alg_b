#include <stdio.h>
#include <stdbool.h>



int main(){
    int numberToTest = 9;
    int operation = numberToTest % 2;
    bool hasModule = ( result > 0 );

    if (hasModule) {

    } else {

    }
    
}